Y
dim(ker(A) + dim(image(A) = dim(V).
dim(Y) = m \ge dim(image(A) \ge dim(V)= n - dim(W) = k.
A:
\{e1, e2, \ldots , ek\}
W.
\{e1, e2, \ldots, en  \}
\{ f1, f2, \ldots fm\}
Y.
Ae1 = Ae2 = \ldots = 0,
A(x1e1 + \ldots xnen) = x1 Ae1 + \dots xn Aen.
