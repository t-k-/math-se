\begin{array}{rl} \text{maximize} & (n+\ell+x_1)\cdots (n+\ell+x_{k-1})(\ell + x_k) \\ \text{subject to} & 0 \leq x_1, ..., x_k \leq n \\ & x_1 + \cdots + x_k = n \end{array} 
\ell \neq n
\ell
n
P \colon \mathbb{R}^k \to \mathbb{R};\;\; P(y_1,\,\dotsc,\, y_k) = \prod\limits_{i=1}^k y_i
S > 0
P
\Sigma(S) = \{ \mathbf{y} : \sum_{i=1}^k y_i = S, y_i \geqslant 0\}
\mathbf{y}_S = (S/k,\,\dotsc,\,S/k)
\Sigma(S)
\partial\Sigma(S) = \{\mathbf{y} \in \Sigma(S) : (\exists i)(y_i = 0)\}
P
P
\Sigma(S)
\mathbf{y}_S \neq \mathbf{y}\in \Sigma(S)
i < j
y_i \neq y_j
y_iy_j = \left(\frac{y_j+y_i}{2} - \frac{y_j-y_i}{2}\right)\left(\frac{y_j+y_i}{2} + \frac{y_j-y_i}{2}\right) = \left(\frac{y_j+y_i}{2}\right)^2 - \left(\frac{y_j-y_i}{2}\right)^2 < \left(\frac{y_j+y_i}{2}\right)^2
P(\mathbf{y}) = \left(\prod_{m \notin\{i,\,j\}} y_m\right)\cdot y_iy_j < \left(\prod_{m \notin\{i,\,j\}} y_m\right)\cdot \left(\frac{y_j+y_i}{2}\right)^2 = P(\mathbf{y}').
\mathbf{y}_S
\mathbf{y}
y_i = n + \ell + x_i
i < k
y_k = \ell + x_k
P
y_i \geqslant n + \ell,\; i < k; \quad y_k \geqslant \ell
\sum_{i=1}^k y_i = (k-1)\cdot(n+\ell) + \ell + \sum_{i=1}^k x_i = k\cdot(n+\ell).
\Sigma(k(n+\ell))
\mathbf{y}_{k(n+\ell)}
x_i = 0
i < k
x_k = n
P
