P\approx BIN(n,p)
Q\approx BIN(n,q)
p(x)
q(x)
P
Q
P
P\approx BIN(n,p)
Q\approx BIN(n+1,q)
P\approx BIN(n,p)
Q\approx BIN(n-1,q)
D_{KL}(P||Q)=\mathbb{E}(log(\frac{p(x)}{q(x)}))
log((\frac{p}{q})^{np})+log((\frac{1-p}{1-q})^{n-np})
