 \lim_{x\to 0} \frac{\ln(\tan(\pi/4 + ax))}{\sin(bx)} 
 \frac{1}{\tan(\pi/4 + ax)}\sec^2(\pi/4 + ax)a. 
 \cos(bx)b 
 \lim_{x\to 0} \frac{\sec^2(\pi/4 + ax)a}{\tan(\pi/4 + ax)\cos(bx)b} = \frac{a\sec^2(\pi/4)}{b\tan(\pi/4)}. 
x\to0
\displaystyle \sin(bx)\sim bx,\quad \ln\big(\tan(\pi/4+ax)\big)=\ln\bigg(1+\frac{2\tan(ax)}{1-\tan(ax)}\bigg)\sim \frac{2\tan(ax)}{1-\tan(ax)} \sim \frac{2ax}{1-\tan(ax)}
\displaystyle\lim_{x\to0}\frac{\ln\big(\tan(\pi/4+ax)\big)}{\sin(bx)}=\lim_{x\to0}\frac{2ax}{bx\big(1-\tan(ax)\big)}=\frac{2a}{b}
