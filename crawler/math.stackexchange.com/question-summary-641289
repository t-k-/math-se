A = \{1, 2, 3, \dots, n\}
k
A_1 = \{a_1, a_2, \dots, a_{m_1}\}
A_2 = \{a_{m_1 + 1}, a_{{m_1} + 2}, \dots, a_{m_2}\}
.
.
.
A_k = \{a_{m_{k-1}+1}, a_{m_{k-1}+2}, \dots, a_n\}
a_i \in A
A_i = \{a_{m_{i-1} + 1}, \dots, a_{m_i}\}
a_{m_{i-1} + 2} - a_{m_{i-1} + 1}
a_{m_{i-1} + 3} - a_{m_{i-1} + 2}
.
.
.
a_{m_i} - a_{m_i - 1}
A_1, A_2, \dots, A_k
