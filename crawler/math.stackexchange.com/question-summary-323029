x
\alpha
\vdash (\forall x \beta \to \alpha) \leftrightarrow \exists x (\beta \to \alpha)
\to
x
\alpha
α→∀x α
\forall x \alpha \to \alpha
\alpha_x^x = \alpha
\vdash (\forall x \beta \to \forall x \alpha) \rightarrow \exists x (\beta \to \alpha)
∀ x(α→β)→( ∀ x α→∀x β)
\vdash \forall x (\beta \to   \alpha) \rightarrow (\exists x (\beta \to \alpha))
∀ x ϕ→∃x ϕ
\to
\leftarrow
\Gamma \vdash ϕ
x
\Gamma
\Gamma \vdash ∀ x ϕ
\Gamma ; γ \vdash ϕ,
\Gamma \vdash  γ \to ϕ
\alpha
\alpha
\exists x (\beta \to \alpha)
c
x
\alpha_c^x=\alpha
x
\alpha
\alpha
\beta_c^x
\forall x \beta
\forall x \beta \to \alpha
\beta_c^x \to \alpha,
\vdash (\forall x \beta \rightarrow \alpha) \leftrightarrow \exists x (\beta \rightarrow \alpha)
x
\alpha
\vdash (\alpha \rightarrow \exists x \beta) \leftrightarrow \exists x (\alpha \rightarrow \beta)
x
\alpha
\lnot \alpha / \alpha
\lnot \beta / \beta
\vDash_{TAUT} (\lnot \phi \rightarrow \psi) \leftrightarrow (\lnot \psi \rightarrow \phi)
\phi \leftrightarrow \psi, \psi \leftrightarrow \sigma \vDash_{TAUT} \phi \leftrightarrow \sigma
\exists
\vdash (\forall x \beta \rightarrow \alpha) \leftrightarrow \exists x (\beta \rightarrow \alpha)
\vdash (\alpha \rightarrow \exists x \beta) \leftrightarrow \exists x (\alpha \rightarrow \beta)
x
\alpha
\alpha \rightarrow \exists x \beta \vdash \exists x (\alpha \rightarrow \beta)
\exists x (\alpha \rightarrow \beta) \vdash (\alpha \rightarrow \exists x \beta)
