x,y\in P
x,y\in X
X
P
X
\le \in P
\le' \in P
x,y\in P
x\le y \Rightarrow x\le' y
\le \preceq\le' 
\le
\le ' 
\preceq
P
x\le y \rightarrow x\le y
\le \preceq \le
x\le y \rightarrow x\le' y
x\le' y \rightarrow x\le y
x\le y  \leftrightarrow x\le' y
x,y
\le = \le'
\preceq
x\le y \rightarrow x\le' y
x\le' y \rightarrow x\le'' y
x\le y\rightarrow  x\le'' y 
\le \preceq \le'
\le' \preceq \le''
\le \preceq \le''
x,y
P
X
X
\le_\varnothing \notin P
P
\le
X
\le'
\le
\le'
P
X= \varnothing
\le_\varnothing \in P
\le_\varnothing 
X\not= \varnothing
x\in X
\forall y,z\in X
y\le_X z \iff (y\not= x \text{ and } y =z) \text{ or } z = x
X
y\le_X y
y\le_X z
z\le_X y
z=x
y\not= x
y=z
z=x
z\le_X y
y=x
y=x=z
y\not=x
y=z
y\le_X z
z\le_X w
z=x
w=z=x
z\le_X w
y\not= x
y=z
z\not= x
z=w
x\le_X w
\le_X \in P
P\not= \varnothing
\mathcal{C}
P
\le_\mathcal{C}
x,y\in X, \;x\le_\mathcal{C} y \iff  \exists \le'\in \mathcal{C}
x\le' y 
\le_\mathcal{C} 
\le, \le' \in \mathcal{C}
x,y \in X
x\le y
y\le' x
y\not= x
\mathcal{C}
\le \preceq \le'
\le' \preceq \le
\le \preceq \le'
x\le y
x\le' y
x\not= y
x<' y
y<' x
\le_\mathcal{C} 
X
\le_\mathcal{C}
\mathcal{C}
x\le' y
\le' \in \mathcal{C}
x\le_\mathcal{C} y
\le' \preceq \le_\mathcal{C}
X
\le_P
\le_P
X
\le_P
X
x,y\in X
x\not\le_p y
y\not\le_p x
\le = \le_p
X\backslash \{x,y\}
x\le y
X
\le \in P
\le_p \preceq \le
x\le_p y \rightarrow x\le y
\le_p
X
P
X
P
X
\le \in P
\le' \in P
x,y\in X
x\le y \Rightarrow x\le' y
\le \preceq\le' 
\le
\le ' 
\preceq
P
x\le y \rightarrow x\le y
\le \preceq \le
x\le y \rightarrow x\le' y
x\le' y \rightarrow x\le y
x\le y  \leftrightarrow x\le' y
x,y
X
x\le y
x\le'y
\le = \le'
\preceq
\le \preceq \le'
\le' \preceq \le''
x\le y \rightarrow x\le' y
x\le' y \rightarrow x\le'' y
x\le y\rightarrow  x\le'' y 
\le \preceq \le''
P
X= \varnothing
\le_\varnothing \in P
P= \{\le_\varnothing  \}
\le \in P
\forall x,y\in X
x \le y \leftrightarrow x\le_\varnothing y
\le
X\not= \varnothing
=\in P
P
=
X
\forall x,y\in X
x = y \rightarrow x\le y
=\preceq \le
\le \in P
P
P
X
\exists x',y'\in X
x'\le y'
y'\le x'
\le' 
\le' = \le
x,y\in X\backslash \{ x',y'\}
x\le y
x',y'
x'\le' y'
\le'
X
\forall x,y\in X
x\le y \rightarrow x\le' y
x=x'
y=y'
\le \preceq \le'
\le \not= \le'
\le
\le
X
\le'
\le
\le'
\le \in P
\mathcal{C}
\le
\le
\le' \in \mathcal{C}\iff \le'\preceq \le 
\le\preceq \le' 
\le \in \mathcal{C}
\mathcal{C}
\mathcal{F}
\mathcal{C}
\le_\mathcal{F}
x\le_\mathcal{F} y \iff \exists \le' \in \mathcal{C}
x\le' y
\le_\mathcal{F} 
\le', \le'' \in \mathcal{F}
x,y \in X
x\le' y
y\le'' x
y\not= x
\mathcal{F}
\le' \preceq \le''
\le'' \preceq \le'
\le' \preceq \le''
x\le' y
x\le'' y
x\not= y
x<'' y
y<'' x
\le_\mathcal{F} 
X
\mathcal{C}
\le_\mathcal{C}
X
\forall x,y \in X
x \le y \rightarrow  x\le_\mathcal{C} y
\le
\le_\mathcal{C} 
\le_\mathcal{C}  \in \mathcal{C}
\le_\mathcal{C} \preceq \le 
\le\preceq \le_\mathcal{C} 
\le\preceq \le_\mathcal{C} 
