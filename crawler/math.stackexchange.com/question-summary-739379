\theta \in \mathbb R
A=\left\lbrace\begin{bmatrix}\cos(\theta)\\\sin(\theta)\end{bmatrix}, \begin{bmatrix}-\sin(\theta)\\ \cos(\theta)\end{bmatrix}\right\rbrace
\mathbb R^2
\langle, \rangle
\rho \in \mathbb R
\begin{bmatrix}\cos(\rho+\theta)\\\sin(\rho+\theta)\end{bmatrix}
A
c_1 =\cos(\theta)\cos(\rho+\theta)+\sin(\theta)\sin(\rho+\theta) = \cos(p + 2\theta)
c_2 =\sin(\rho+\theta)\cos(\theta)-\sin(\theta)\cos(\rho+\theta) = \sin(p + \theta-\theta) = \sin(\rho)
\rho, \theta
\left[ \begin{array}{c} \cos(\rho+\theta) \\ \sin(\rho+\theta)  \end{array} \right]= \left[ \begin{array}{c} \cos(\rho)\cos(\theta)- \sin(\rho)\sin(\theta)\\ \sin(\rho)\cos(\theta)+ \cos(\rho)\sin(\theta)  \end{array} \right]=\cos(\rho)\left[ \begin{array}{c} \cos(\theta) \\ \sin(\theta)  \end{array} \right]+\sin(\rho)\left[ \begin{array}{c} -\sin(\theta) \\ \cos(\theta)  \end{array} \right]
