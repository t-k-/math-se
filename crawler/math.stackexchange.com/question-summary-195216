\phi
\exists y R(x, y)
\cal M
\cal N
T
\cal M \subset N
b, c \in M
{\cal M} \models R(b, c)
{\cal M} \models P(b)
{\cal N} \models \exists y R(b, y) \land P(b)
{\cal N} \not \models \forall x (\exists y R(x, y) \leftrightarrow \lnot P(x))
{\cal N} \models \exists y R(a, y)
a \in M
{\cal N} \models P(a)
{\cal M} \models P(a)
{\cal M} \models \exists y R(a, y)
\psi
\psi
\begin{array} {rl} T = \{ & \forall x (\exists y R(x, y) \leftrightarrow P(x)) \lor \forall x (\exists y R(x, y) \leftrightarrow \lnot P(x)), \\ & \exists x, y R(x, y)\} \end {array}
{\cal M} \models \forall x(\exists y R(x, y) \leftrightarrow P(x)) \lor \forall x (\exists y R(x, y) \leftrightarrow \lnot P(x)).
{\cal M} \models \forall x(\exists y R(x, y) \leftrightarrow P(x)).
{\cal N} \models \forall x (\exists y R(x, y) \leftrightarrow P(x)).
T \models \forall x(\exists y R(x, y) \leftrightarrow \psi(x))
T \models \forall x \Bigg(\exists y R(x, y) \leftrightarrow \forall u, v \bigg(\Big(\big(R(u,v) \to p(u)\big) \land p(x) \Big) \lor \Big(\big(R(u,v) \to \lnot p(u) \big) \land \lnot p(x)\Big)\bigg)\Bigg)
\chi
\chi^*
S_{\chi}
\theta
T,\chi \vdash \theta
T \cup S_{\chi} \cup \{\neg \chi\}
\chi
T \cup S_{\chi}
\chi
T
S_{\chi}
T
\chi
T
T \cup S_{\chi} \cup \{\neg \chi\}
A
T
