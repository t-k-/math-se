(M_\alpha)_\alpha
\prod_{\alpha}M_\alpha
\mathbf{C}
\mathcal{S}et
F\colon\mathbf{D}\to\mathbf{C}
\pi_X
P
F(X)
p_{X,Y,f}
P'
F(Y)
P = \prod_{X\in\mathrm{Ob}(\mathbf{D})} F(X)\qquad\text{and}\qquad P' = \prod_{\stackrel{X,Y\in\mathrm{Ob}(\mathbf{D})}{f\in\mathbf{D}(X,Y)}} F(Y).
a,b\colon P\to P'
p_{X,Y,f}a
p_{X,Y,f}b
a
b
a
p_{X,Y,f}a = \pi_Y
P
F(Y)
F(Y)
F(Y)
(X,Y,f)
P
P'
b
p_{X,Y,f}b = F(f)\pi_X
P
F(X)
F(X)
F(Y)
F(f)
k\colon L\to P
a
b
L
k
\pi_Xk\colon L\to F(x)
F(f)
L
\pi_Xk
\lim\limits_{\leftarrow} F
L
F
\Box
\mathbf{C}
\mathcal{S}et
\mathcal{G}roup
\Delta\colon\mathbf{C}\to\mathbf{C}^{\mathbf{D}}
X
\Delta(X)\colon\mathbf{D}\to\mathbf{C}
X
\mathrm{id}_X
f\colon X\to Y
\mathbf{C}
\Delta(f)\colon\Delta(X)\to\Delta(Y)
f
\mathbf{D}
F\colon\mathbf{D}\to\mathbf{C}
L
\mathbf{C}^{\mathbf{D}}(\Delta(-),F)\colon\mathbf{C}^{\rm op}\to\mathcal{S}et
\mathbf{D}\to \mathbf{C}
\lim\limits_{\leftarrow}
\Delta\colon \mathbf{C}\to\mathbf{C}^{\mathbf{D}}
