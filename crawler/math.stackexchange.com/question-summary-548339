x^y=y^x
x^y\log(x) \cdot y' = y^x\log(y)\cdot1;  
y'=\frac{y^x\log(y)}{x^y\log(x)}
\log
 \begin {eqnarray*} x^y &=& y^x \\ y \cdot \log (x) &=& x \cdot \log (y) \\ y' \cdot \log(x) + \dfrac {y}{x} &=& \dfrac {x}{y} \cdot y' + \log (y) \\ y' \cdot \left( \log(x) - \dfrac {x}{y} \right) &=& \log(y) - \dfrac {y}{x} \\ y' &=& \dfrac {\log (y) - \dfrac {y}{x}}{\log (x) - \dfrac {x}{y}}, \end {eqnarray*} 
 y' = \boxed {\dfrac {x \log (y) - y}{y \cdot \log (x) - x}}. 
 \frac{d}{dx} y^x 
 \frac{d}{dx} a^x = a^x \ln (a), 
y
a
a
y
x
y^x = e^{x \ln (y)}
