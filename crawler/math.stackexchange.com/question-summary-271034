\vartheta(\beta)=g(\beta)+\theta(\beta),
\beta
\vartheta(\beta)
m
g(\beta)+\theta(\beta)=0
m
g(\beta)=-\theta(\beta)
m
r_1, r_2, r_3, \cdots r_m
\vartheta(r_k) = 0
 \vartheta(\beta) = g(\beta) + \theta(\beta)
\beta = r_k
 \vartheta(r_k) = g(r_k) + \theta(r_k) 
 0 = g(r_k) + \theta(r_k) 
 -g(r_k) = \theta(r_k) 
k
1
m
\vartheta(\beta)=g(\beta)+\theta(\beta)
\beta
\vartheta(\beta)=0
g(\beta)=-\theta(\beta)
\vartheta(\beta)=0\iff g(\beta)+\theta(\beta)=0\iff g(\beta)=-\theta(\beta),
