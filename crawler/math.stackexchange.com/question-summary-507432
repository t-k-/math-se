\sum\limits c_n
k_n
\sum\limits c_nk_n
k_n
M>0
\vert k_n \vert \leq M
\vert \sum\limits c_n k_n \vert \leq \sum\limits \vert c_nk_n\vert \leq M\sum\limits \vert c_n \vert < M\epsilon
\sum\limits\vert c_nk_n\vert 
\sum_j c_j
\epsilon > 0
N(\epsilon)
|\sum_{j=m}^n c_j| < \epsilon
N(\epsilon) < m < n
|k_j| < M
|\sum_{j=m}^n k_j c_j| < M\epsilon
N(\epsilon) < m < n
\epsilon
M \epsilon
\epsilon
\epsilon/M
|\sum_{j=m}^n k_j c_j| < \epsilon
N(\epsilon/M) < m < n
\vert \sum\limits c_n k_n \vert \leq \sum\limits \vert c_nk_n\vert \leq M\sum\limits \vert c_n \vert < M\epsilon
n=M,M+1,\dots
