\vartheta(x)= \sum_{p \leq x} \log p.
\sum_{p \leq x} \log p \sim x 
\sum_{n\leq x} \log n \approx \int_2^x \log t\hspace{2mm}dt \approx x \log x - x.
\frac{\sum \log p}{\sum \log n} \approx \frac{\sum \log p}{x\log x - x}\sim \frac{x}{x\log x - x }\sim \frac{1}{\log x } \sim \frac{\pi(x)}{x}.
(1).....\hspace{5mm}\frac{\sum \log p_k}{\sum \log n}\hspace{2mm} ?\sim \frac{\pi_k(x)}{ x}.
\pi_5 = 963,
\frac{\pi_5(x) }{x} = 0.0963
\frac{\sum \log p_5}{\sum \log 10^4} \approx 0.0974
(2)...\hspace{4mm}\frac{\pi_k(x)}{\sum \log p_k} ? \sim \frac{1}{\log x}.
 \sum_k \pi_k(x) = x,
(3)...\hspace{4mm}\sum_k \sum_{p_{k}(m) \leq x} \log p_{k}(m)  \sim x \log x. 
m = 1,2,3,... 
 p_k(m) \leq x
p_k \leq x
\pi_2(x) \approx \frac{\sum \log p_2}{\log x}
\frac{\pi_k(x) = m}{\sum_{n=1}^m \log p_k(n)}\sim \frac{1}{\log x},
 \frac{(1/m) m }{(1/m) \log  \prod p_k(m)}\sim \frac{1}{\log x} 
  \sqrt[m]{p_k(1)p_k(2)...p_k(m)} \sim x \hspace{4mm}...?
(B)
(A)
2
(A)
(A)
\pi_k(x)\sim\sum_{p_k\leq x}1 \sim \frac{x (\log \log x)^{k-1}}{(k-1)!\log x}\ \ \ \ \ \ \ \ \ \ (A)
\vartheta_k(x)\sim\sum_{p_k\leq x}\log (p_k) \sim \frac{x (\log \log x)^{k-1}}{(k-1)!},\ \ \ \ \ \ \ \ \ \ (B)
\theta_k(x)\sim \pi_k(x)\log x.
\sum_{p_k\leq x} \log p_k=\int_1^x \log t d\left(\pi_k(t)\right),
\pi_k(x)\log x-\int_{1}^x\frac{\pi_k(t)}{t}dt.
