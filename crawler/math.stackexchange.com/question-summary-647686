x=(x_1,\dots,x_n)
n
V
s
n
S_n
s(x)=(x_{s(1)},\dots,x_{s(n)})
A={1 \over n!}\sum_{s\in S_n}\text{sgn}(s)s
S={1 \over n!}\sum_{s\in S_n}s
\sum_{s\in S_n}\sum_{p\in S_n}\text{sgn}(p)sp=\sum_{t\in S_n}t\sum_{p\in S_n}\text{sgn}(p)
t=sp
t
p\in S_n
t\in S_p
s\in S_n
sp=t
s=tp^{-1}
\sum_{s\in S_n}\left(\sum_{p\in S_n}\mathrm{sgn}(p)\cdot sp\right) = \sum_{p\in S_n}\left(\mathrm{sgn}(p)\cdot\sum_{s\in S_n}sp\right).
\sum_{s\in S_n}sp = \sum_{t\in S_n}t
\sum_{p\in S_n}\mathrm{sgn}(p)
V
\sum_{p\in S_n}\left(\mathrm{sgn}(p)\cdot\sum_{t\in S_n}t\right) = \sum_{t\in S_n}\left(t\sum_{p\in S_n}\mathrm{sgn}(p)\right),
\sum_{p\in S_n}\left(\mathrm{sgn}(p)\cdot\sum_{t\in S_n}t\right) = \left(\sum_{p\in S_n}\mathrm{sgn}(p)\right)\cdot\sum_{t\in S_n}t = 0\cdot\sum_{t\in S_n}t = \underline{0}
