d \theta
\theta
\frak{F} \mathrm{(M)}
X, Y, V, W \in \frak{X} \mathrm{(M)}
f, g, h, k \in \frak{F} \mathrm{(M)}
d \theta (X,Y) = X \theta Y - Y \theta X - \theta [X,Y]
d \theta (fX + gY, hV + kW) = fh d \theta (X, V) + fk d \theta (X, W) + gh d \theta (Y,V) + gk d \theta (Y,W)
d \theta (X,Y)=-d\theta(Y,X)
d\theta(fX+gY,V)=f d\theta(X,V)+gd\theta(Y,V)
d \theta (fX + gY, V) = f d \theta (X, V) +g d \theta (Y,V),
d \theta (X, hV + kW) = h d \theta (X, V) + k d \theta (X,W).
