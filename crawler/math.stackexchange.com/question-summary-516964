(\lambda x.\lambda y. x)
y
\alpha
y
(\lambda x.\lambda y. x) \alpha = \alpha
(\lambda x.\lambda y. x)y = \lambda y.y
\lambda
(\lambda x.\lambda y. x)y = (\lambda y. x)[x := y]
(\lambda x.\lambda y. x)y = (\lambda y. x)[x := y] = (\lambda z. x)[x := y] = (\lambda z. y)
