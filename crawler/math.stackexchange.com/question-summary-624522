ax=b+\max(cx,d)
a,b,c
d
cx \geq d
 a x = b + c x \Rightarrow x = \frac{b}{a-c}, \text{ and } c x= \frac{bc}{a-c} \geq d
cx \leq d
 a x = b + d \Rightarrow x = \frac{b+d}{a}, \text{ and } c x= \frac{c(b+d)}{a} \geq d
\frac{bc}{a-c} \geq d
\frac{c(b+d)}{a} \geq d
x <\frac{d}{c}
max(cx,d)=d
d>cx
x=\frac{d}{c}
max(cx,d)
d
cx
x>\frac{d}{c}
d<cx
x<=\frac{d}{c}
ax=b+d
max(d,cx)=cx
x>\frac{d}{c}
ax=b+cx
a-c\not=0
c\gt0
cx\ge d
x\ge d/c
x
\ e = 1\ 
\,cx > d\, 
\,e = 0.\,
\ ax = b+max(cx,d) = b+ ecx + (1\!-\!e)d, \ 
 x = \frac{b+(1\!-\!e)d}{a-ec}
