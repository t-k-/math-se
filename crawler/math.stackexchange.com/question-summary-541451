B: X_1 \times X_2 \rightarrow Y
L(X,Y)
B: X_1 \times X_2 \rightarrow Y
B(x_1 + h_1 , x_2 + h_2) = B(x_1,x_2) + B(h_1,x_2) + B(x_1,h_2) + B(h_1,h_2)
\frac{B(h_1,h_2)}{|(h_1,h_2)|} \rightarrow 0
L^2(X_1,X_2;Y)
B \colon X_1 \times X_2 \to Y
\lVert B(x,y)\rVert \leqslant \lVert B\rVert\cdot \lVert x\rVert\cdot \lVert y\rVert
x,y
\lVert B\rVert = \sup \left\lbrace \lVert B(x,y)\rVert : \lVert x\rVert \leqslant 1, \lVert y\rVert \leqslant 1\right\rbrace
B
\frac{B(h_1,h_2)}{\lvert (h_1,h_2)\rvert}\to 0
X_1\times X_2
\lvert (x,y)\rvert = \max \{\lVert x\rVert,\, \lVert y\rVert\}
\lvert(x,y)\rvert = \lVert x\rVert + \lvert y\rVert
\left(\lVert x\rVert^p + \lVert y\rVert^p\right)^{1/p}
(h_1,h_2) \neq (0,0)
\lVert B(h_1,h_2)\rVert \leqslant \lVert B\rVert \cdot \lVert h_1\rVert \cdot \lVert h_2\rVert \leqslant \lVert B\rVert \cdot \lvert (h_1,h_2)\rvert^2,
