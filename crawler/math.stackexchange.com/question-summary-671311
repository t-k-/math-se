V
A
\sup
\max
\lvert\lvert A \rvert\rvert=\sup_{x\in V}\frac{\lvert\lvert Ax \rvert\rvert}{\lvert\lvert x \rvert\rvert},
\lvert\lvert A \rvert\rvert=\max_{x\in V}\frac{\lvert\lvert Ax \rvert\rvert}{\lvert\lvert x \rvert\rvert},
1
\lVert A\rVert = \sup_{\lVert x\rVert = 1} \lVert Ax\rVert.
x\mapsto \lVert Ax\rVert
