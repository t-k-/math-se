\sup(\emptyset)=\min(S)
\inf(\emptyset)=\max(S)
\min(S)
\max(S)
S
K
b
k\in K
k\leq b
K
k\in K
k\leq b
k
\lt
\gt
S
\emptyset
\sup(\emptyset)
\sup(\emptyset)=\min(
\emptyset) = \min(S)
S
\emptyset
\inf(\emptyset) = \max(
\emptyset) = \max(S)
\sup(\emptyset)\leq\inf(\emptyset)
\sup(\emptyset)\lt \inf(\emptyset)
\operatorname{sup}(\emptyset) = -\infty < \infty = \operatorname{inf}(\emptyset)
S
T
\mathbb{R}
S \subset T
\operatorname{inf}(T) \leq \operatorname{inf}(S)
\operatorname{sup}(T) \geq \operatorname{sup}(S)
\;
\ \ \rm x \le S \;\iff\; x \le s,\;\: \forall\: s \in S,\;
\rm\; x \ge S
\;\rm S = \emptyset\;
\;\rm x \le S\;
\;\rm x \le \min \emptyset\;
\;\rm x\;
\;\rm \min \emptyset = \infty\;
\;\rm \max\emptyset = -\infty\;
\;\rm a,b\ |\ [a,b] \;
\;\rm c = [a,b] \;
\;\rm (a,b)\ |\ a,b \;
\iff
\iff
