+_{\mathbb{N}}
\mathbb{N}
a > b \leftrightarrow \exists c (a = b +c')
a \ge b
a > b \lor a = b
b \ge a
b > a \lor a = b
a = b
a > b
b > a
a > b
b > a
a > a
a = a + e'
\lnot A \supset (A \supset B)
a = b
a
\varphi(x) \equiv (x \geq b ~\land~ b \geq x) \rightarrow (x=b)
\phi(0)
\checkmark
\fbox{1}~\varphi(n) \equiv (n \geq b ~\land~ b \geq n) \rightarrow (n=b).
\varphi(n')\equiv (n' \geq b ~\land~ b \geq n') \rightarrow (n'=b),
\fbox{2}~(n' \geq b ~\land~ b \geq n')
(n' = b)
\fbox{2a}~n' \geq b; \\ \fbox{2b}~b \geq n'.
\fbox{2a}~(n' \geq b) \equiv (\exists \alpha)~ n' = b + \alpha; \\ \fbox{2b}~(b \geq n') \equiv (\exists \beta)~ b = n' + \beta.
(\exists \alpha, \beta)~ n' = n' + \beta + \alpha; \\ (\exists \alpha, \beta)~ n + 1 = n + 1 + \beta + \alpha; \\ (\exists \alpha, \beta)~ n + 1 = n + \beta + \alpha + 1; \\ (\exists \alpha, \beta)~ n + 1 = n + (\beta + \alpha)'.
\forall a,b,c \in \mathbb{N}: [a + b = a + c] \Longrightarrow b = c
1 = (\beta + \alpha)'.
(1 := 0')
\fbox{3}~\alpha + \beta = 0.
n' = b
\alpha = \beta = 0
(n' \not = b) \\\equiv \lnot (\alpha = \beta = 0) \\\equiv (\alpha \not= 0 \lor \beta \not= 0).
\rhd
(\alpha \not= 0)
\alpha
\eta
(\exists \eta \in \mathbb{N})~\eta' = \alpha
(\exists \eta \in \mathbb{N})~\eta' + \beta = 0
+
(\exists \eta \in \mathbb{N})~(\eta + \beta)' = 0
0
(\eta + \beta) \in \mathbb{N}
\rhd
(\beta \not= 0)
\bot
\lnot(\alpha \not= 0 \lor \beta \not= 0) \\ \equiv (\alpha = 0 \land \beta = 0) \\ \equiv (\alpha = \beta = 0).
n' = b
\checkmark
