[a]_{n} = [1]_{n}
\mathrm{gcd}(a, n) = 1
a \equiv 1 \pmod{n}
1 \equiv a \pmod{n}
\mathrm{gcd}(a, n) := d  
\mathrm{gcd}(1, n) := d'.
 d = d'
\mathrm{gcd}(a,n) = \mathrm{gcd}(1, n)
\mathrm{gcd}(a, n) = 1
d = as + nt,  \quad s, t \in \mathbb{Z}
d' = as' + nt',  \quad s', t' \in \mathbb{Z}.
[a]_n = [1]_n
1 = a+kn
k
1
a
n
\gcd(a,n)
1
