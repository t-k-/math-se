y(x)= \int_{0}^{x}f(t)\sin{(px-pt)}dt
y''(x)-((p^2)*y(x))
\sin(px-pt)
e^{i(px-pt)}
\sin(px-pt)=\sin(px)\cos(pt)-\cos(px)\sin(pt).
 y(x)=\sin(px)\int_0^xf(t)\cos(pt)dt - \cos(px)\int_0^xf(t)\sin(pt)dt.
