\lambda
(\lambda x.\lambda y.x)(\lambda x.x)((\lambda x.x x)(\lambda x.x x))
x \leadsto y
(\lambda x. xx)
(\lambda y. yy)
(\lambda y. yx)
(\lambda y. yy)(\lambda x. xx)
yy
y
(\lambda x. xx)
(\lambda x. xx)(\lambda x. xx)
\color{blue}{\text{blue}}
\color{red}{\text{red}}
\ddot\smile
