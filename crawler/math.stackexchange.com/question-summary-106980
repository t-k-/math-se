\varphi(v_k / v_l )
\varphi(v_l / v_k )
[\exists v_k \varphi(v_k)] = [\exists v_l \varphi(v_l)]
[\varphi]
\varphi \sim \psi \iff T \vdash \varphi \leftrightarrow \psi
\leq
T \vdash \exists v_k \varphi (v_k) \rightarrow \exists v_l \varphi(v_l)
T \cup \{ \exists v_k \varphi(v_k) \} \vdash \exists v_l \varphi(v_l)
T \cup \{ \exists v_k \varphi (v_k) \} \vdash  \exists v_k \varphi (v_k)
T \cup \{ \exists v_k \varphi (v_k) \} \vdash  \exists v_l \varphi (v_l)
T \vdash \exists v_k \varphi (v_k) \rightarrow  \exists v_l \varphi (v_l)
[\exists v_k \varphi (v_k)] \leq [\exists v_l \varphi (v_l)]
\geq
\varphi(t) \rightarrow \exists x \varphi(x)
\forall x (\varphi(x)\to \psi)\to (\exists x \varphi(x)\to \psi))
y
x
(\exists x)\phi(x)
(\exists x)\phi(x)
(\exists x)\phi(x)
(\exists y)\phi(y)
y
x
\phi
L_{15}
L_{15}
(\exists y)\phi(y)
x
\phi(x)
y
x
(\exists y)\phi(y)
(\forall x)[\phi(x)\to (\exists y)\phi(y)]
(\exists x)\phi(x) \to (\exists y)\phi(y)
 (\forall x)[\phi(x) \to (\exists y)\phi(y)] \to [(\exists x)\phi(x) \to (\exists y)\phi(y)] 
(\forall x)[\phi(x)\to (\exists y)\phi(y)]
L_{13}
\phi(x) \to (\exists y)\phi(y)
L_{13}
y
x
(\forall x)[\phi(x)\to (\exists y)\phi(y)]
z_\phi
\epsilon
\phi(a) \to \psi
a
(\exists x)\phi(x) \to \psi
a
(\exists x)\phi(x)
\phi(a)
 (\forall a)[\phi(a) \to \psi] \to [(\exists x) \phi(x) \to \psi] 
