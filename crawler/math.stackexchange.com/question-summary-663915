d
U \subset \mathbb{R}^n
\ell_{\infty}
v_1, \ldots, v_d
U
x_i = (v_{1i}, \ldots, v_{di})^\intercal \in \mathbb{R}^d
i \in \{1, \ldots, n\}
x_i
i
 \max_i ||x_i||^2 \le \sum_{j=1}^d ||v_j||_{\infty}^2 \le \max_i ||x_i||^2 O(\textrm{polylog(d)}) 
\{v_i\}
U
U
U = \mathbb{R}^n
 \sum_j \|v_j\|_\infty^2 \leq C(n) \max_i \|x_i\|_2^2, 
C(n) = O(\log n)
C(n) = O(1)
U
k = 2^{\lfloor \log_2 n \rfloor}
v_1, \ldots, v_k
k
k^{-1/2}
n
\text{span}\{e_1, \ldots, e_k\}
\text{span}\{e_{k+1}, \ldots, e_n\}
n-k < n/2
\lceil \log_2 n\rceil
\sum_j{\|v_j\|_\infty^2} = O(\log n)
\max_i \|x_i\|_2^2 = 1
4r
r
k = 4 \lfloor n/4 \rfloor
