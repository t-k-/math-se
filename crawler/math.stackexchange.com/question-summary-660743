x
S(x)=
\text{x is a student}
M(x)=
\text{x visited Mexico}
\forall x(S(x) \rightarrow M(x))
\forall x(S(x) \land  M(x))
x
S(x)
\to
\exists x(S(x)\to M(x))
 \begin{array}{cc|c} A&B& A\to B \\ \hline T & T & T \\ T & F & F \\ F & T & T \\ F & F & T \end{array} 
\exists x
\lnot\forall x\lnot
S(x)\to M(x)
\lnot S(x)\lor M(x)
 \forall x(S(x)\land \lnot M(x)) 
\forall x\neg\Phi
\exists x \Phi
 \forall x(S(x)\to\neg M(x))
 \forall x(\neg(S(x)\land M(x))
 \exists x(S(x)\land M(x)).
