\Sigma
\phi, \psi
\Sigma \cup \lbrace \phi \rbrace \models \psi
~\Sigma \models \phi \rightarrow \psi
\models
\lbrace p \rightarrow q, p\rightarrow (q \rightarrow r)\rbrace \cup \lbrace p \rbrace \models r
\lbrace p \rightarrow q \rbrace \cup \lbrace p\rightarrow (q \rightarrow r)\rbrace \models p \rightarrow r
\emptyset \cup \lbrace p \rightarrow q \rbrace \models (p\rightarrow (q \rightarrow r)) \rightarrow (p \rightarrow r)
p \rightarrow r
\emptyset \models p \rightarrow q  \rightarrow [(p\rightarrow (q \rightarrow r)) \rightarrow (p \rightarrow r)]
