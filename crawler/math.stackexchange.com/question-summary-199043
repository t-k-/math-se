A
A^{\dagger}_lA = I
A
AA^{\dagger}_r = I
A^{\dagger}_l = (A^TA)^{-1}A^T,
A^{\dagger}_r = A^T(AA^T)^{-1},
A^{\dagger}_l
A^{\dagger}_r
A^{\dagger}_rA\approx I
A
A
A\in \mathbb{C}^{m\times n}
 \operatorname{rank}(A)\leq\min(m,\,n).
A^TA\in\mathbb{C}^{m\times m}
AA^T\in\mathbb{C}^{n\times n}
\def\Mat{\mathop{\mathrm{Mat}}\nolimits}\def\R{\mathbb R}\def\l{\text{left}}\def\r{\text{right}}\def\rank{\mathop{\mathrm{rank}}\nolimits}
A \in \Mat_{n,m}(K)
A^\dagger_\r
AA^T \in \Mat_n(K)
\rank A = n
n \le m
n < m
A^\dagger_\r A
\rank (A^\dagger_\r A) \le n < m
A^\dagger_r A \not\approx \mathrm{Id}
n = m
\rank A = n
A
A^\dagger_r = A^{-1}
A^\dagger_\r A = \mathrm{Id}
