\mathrm{Power\ set}
(V_\omega,\in)\models \mathrm{Power\ set}
(V_\omega,\in)\models \forall x \exists y \forall z (z\subseteq x \rightarrow z \in y)
a\in V_\omega
P(a)
V_\omega
a\in V_\omega
rk(a)\leq n
n\in \omega
z\in P(a)
z\subseteq a
rk(z)\leq rk(a)
rk(P(a))\leq n+1
P(a)\in V_\omega
P(a)\in V_\omega
a\in V_\omega
\mathrm{Power\ set}
(V_\omega,\in)
a \in  V_\omega
(V_\omega,\in)\models \forall z (z\subseteq a \rightarrow z\in P(a))
a\in V_\omega
z\in V_\omega
(V_\omega, \in)\models z\subseteq a
(V, \in)\models z\subseteq a
(**)
z\subseteq a
(V_\omega,\in)
\Delta_0
V_\omega
(**)
z \in V_\omega
z\in P(a)
(V_\omega,\in)\models \mathrm{Power\ set}
