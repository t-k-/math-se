\kappa > \operatorname{cf}(\kappa)
\kappa
\kappa^{<\kappa} = \kappa^{\operatorname{cf}(\kappa)}
\kappa
2^{<\kappa} = \kappa^{<\kappa} > \kappa
\kappa = \sum_{i < \operatorname{cf}(\kappa)} \kappa_i
\kappa_i < \kappa
\kappa^{<\kappa} = \sum_{\lambda < \kappa} \kappa^{\lambda} = \sum_{i<\operatorname{cf}(\kappa)}\kappa^{\kappa_i}
\kappa^{\lambda} \leq (\sum_{\alpha < \kappa} \alpha^{\lambda})^{\operatorname{cf}(\kappa)}
\sum_{i<\operatorname{cf}(\kappa)}\kappa^{\kappa_i} \leq \sum_{i<\operatorname{cf}(\kappa)}(\sum_{\alpha < \kappa} \alpha^{\kappa_i})^{\operatorname{cf}(\kappa)}
\kappa^{<\kappa}= \kappa^{\operatorname{cf}(\kappa)} > \kappa
2^{<\kappa} = \kappa^{<\kappa}
\mu < \kappa
2^{\mu} \geq \kappa
\alpha^{\kappa_i}<\kappa
\kappa
\alpha^{\kappa_i}\leq (2^\alpha)^{\kappa_i}<\kappa
\lambda
\kappa
\kappa^{<\kappa}\leq \kappa^{\mathrm{cf}(\kappa)}
\kappa^\lambda\leq \sum_{i<\mathrm{cf}(\kappa)}\left(\sum_{\alpha<\kappa}\alpha^{\kappa_i}\right)^{\mathrm{cf}(\kappa)} \leq \sum_{i<\mathrm{cf}(\kappa)}\kappa^{\mathrm{cf}(\kappa)} = \kappa^{\mathrm{cf}(\kappa)}
2^{<\kappa}=\kappa^{<\kappa}
\kappa
2^{<\kappa}>\kappa
\kappa
\kappa\le 2^\lambda
\lambda<\kappa
\lambda\le\mu<\kappa
2^\mu=\left(2^\lambda\right)^\mu\ge\kappa^\mu\ge 2^\mu\;,
2^{<\kappa}=\kappa^{<\kappa}
