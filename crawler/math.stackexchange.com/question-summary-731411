z_1,...,z_n \in \mathbb C
\displaystyle  \forall i  |z_{i}|= 1
\displaystyle u_{k}= \sum_{i=1}^{n} z_{i}^{k}
n
\color{red}{^{[1]}}
(u_{k})
z_j=e^{i \theta_j}
|u_k-n| \leq \sum_{j=1}^n |1-e^{ik \theta_j}| = \sum_{j=1}^n |\int_0^{k \theta_j - 2\pi \lfloor \frac{k \theta_j}{2\pi} \rfloor} e^{it}dt|\leq 2\pi \sum_{j=1}^n k \alpha_j - \lfloor k \alpha_j \rfloor
\mathbb{T}^n = \mathbb{R}^n / \mathbb{Z}^n
x\in \mathbb{R}^n]
[x] = \{x\} + \mathbb{Z}^n
x \sim y
x-y \in \mathbb{Z}^n
k \mapsto [\sum_{j=1}^n k \alpha_j ]
[0] \in \mathbb{T} 
\alpha_j
\mathbb{Q}
\alpha = (\alpha_1,...,\alpha_n)^T
\alpha_j
\mathbb{Q}
q \alpha_j
q \in \mathbb{q}
k \mapsto [k \alpha]
\mathbb{T}^n 
k_i
[k_i \alpha] \to [0]
u_{k_i} \to n
\alpha_1,..., \alpha_l 
\operatorname{sp}_\mathbb{Q} \{ \alpha_1,...,\alpha_n \}
\mathbb{Q}
l<n
j >l
\alpha_i = \gamma_j^T \bar{\alpha}
\gamma_j \in \mathbb{Q}^{n-l}
\bar{\alpha} = ( \alpha_1,..., \alpha_l )^T
p \in \mathbb{Z}
\gamma_j = {1 \over p} \lambda_j
\lambda_j \in \mathbb{Z}^{n-l}
k_i
[k_i\bar{\alpha}] \to [0] \in \mathbb{T}^l
n_i \in \mathbb{Z}^l
k_i \bar{\alpha}+n_i \to 0
\mathbb{R}^l
p \in \mathbb{Z}
p(k_i \bar{\alpha}+n_i) \to 0
[pk_i\bar{\alpha}] \to [0]
\gamma_i^T(p(k_i \bar{\alpha}+n_i)) = \gamma_i^T(pk_i \bar{\alpha})+\lambda_i^T(n_i) = \lambda_i^T (k_i \bar{\alpha}+n_i)
[\gamma_i^T(pk_i \bar{\alpha})] \to [0] \in \mathbb{T}
[p k_i \alpha] \to [0] \in \mathbb{T}^n
u_{(p k_i)} \to n
(v_k)
v_{\phi(k)}
(l_1,l_2,\cdots, l_n)
\psi(k)=\phi(k+1)-\phi(k)
\frac{z_i^{\phi(k+1)}}{z_i^{\phi(k)}}\rightarrow 1
i\in \{1,\cdots,n\}
z_i^{\psi(k)}\rightarrow 1
u_{\psi(k)}\rightarrow n
\square
