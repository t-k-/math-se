\hat \varphi (\omega)
\varphi
*
\hat g(\omega)
\hat f(\omega)
a \in (0,1)
f_2 (x) = xf(x)
 (f_2  * g)(t) = at(f * g)(t), 
(xf) * g = a t (f * g)
(\hat{x}*\hat{f} ) \hat{g} = a\hat{x} * (\hat{f} \hat{g}) \implies (\delta'*\hat{f} ) \hat{g} = a\delta' * (\hat{f} \hat{g}) \implies \hat{f}' \hat{g} = a (\hat{f}\hat{g})'
a \hat{g} ' + (a-1) (\hat{f}' / \hat{f}) \hat{g} =0 
\hat{g} = \hat{f}^{1/a - 1}
\psi = x \varphi
\hat \psi = i \hat \varphi'
 (xf) * g = at(f * g) \Rightarrow \hat f'\hat g = a(\hat f \hat g)'  
