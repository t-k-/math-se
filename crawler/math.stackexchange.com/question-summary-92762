x_1,x_2, \ldots, x_n
F(x)= \left( \frac{x}{\theta} \right)^\beta, \quad 0 \leq x \lt \theta.
β
θ
(1-\alpha)
\beta^2
Y = \max_i X_i
G(y) = P(Y < y) = P\left(\cap_i X_i < y\right) =\prod_i P(X_i < y) = F(y)^n =\left( {y\over\theta} \right)^{n\beta}.
q_1
q_2
\alpha\over2
{1-\alpha\over 2}
G(q_1) = {\alpha \over 2}
n\beta (\log q_1 - \log \theta) = \log\left({\alpha\over 2}\right)
\log(q_1) = \log(\theta) + {1\over n\beta} \log\left({\alpha\over 2}\right)
G(q_2) = 1-{\alpha\over 2}
\log(q_2) = \log(\theta) + {1\over n\beta} \log\left(1-{\alpha\over2}\right)
P(\log q_1 < \log Y < \log q_2) = 1-\alpha
\log Y < \log \theta
P\left(\log\left({\alpha\over 2}\right) < n\beta(\log Y-\log\theta) < \log\left(1-{\alpha\over2}\right)\right) = 1-\alpha,
 P\left({ \log\left({\alpha\over 2}\right)  \over n (\log Y -\log\theta)} > \beta > {\log\left(1-{\alpha\over2}\right) \over n (\log Y -\log\theta)} \right) = 1-\alpha.
q_1
q_2
\xi
1-\alpha+\xi
\xi
\beta^2
x\mapsto x^2
