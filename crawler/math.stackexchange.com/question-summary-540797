x
\int_{-\pi}^\pi xe^{-inx}dx
\int_{-\pi}^\pi xe^{-inx}dx=x\frac{e^{-inx}}{-in}-\int_{-\pi}^\pi\frac{e^{-inx}}{-in}dx
\dfrac{i}{n}(\pi(\cos(-n\pi)+i\sin(-n\pi)+\pi(\cos(n\pi)+i\sin(n\pi))=\dfrac{2\pi i}{n}\cos(n\pi)
-\dfrac{1}{n^2}(\cos(-n\pi)+i\sin(-n\pi)-\cos(n\pi)-i\sin(-n\pi))=\dfrac{2i}{n^2}\sin(n\pi)
n=0
\int_{-\pi}^\pi xdx=0
n
\dfrac{2\pi i}{n}\cos(n\pi)-\dfrac{2i}{n}\sin(n\pi)=\dfrac{2\pi i(-1)^n}{n}
