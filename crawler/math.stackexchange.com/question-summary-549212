(A+B)\times C \cong (A \times C) + (B \times C)
\hom((A\times C)+(B\times C),X) \cong \hom((A+B)\times C,X)
X
\alpha_X : \text{hom}((A\times C) + (B\times C), X) \simeq \text{hom}((A + B) \times C, X)
\beta_X
\alpha_X
\alpha
f : X \to Y
\phi \in \text{hom}((A\times C) + (B\times C), X)
\alpha
\alpha
U = (A + B) \times C
V = (A \times C) + (B \times C)
\text{id} \in \text{hom}(V, V)
\alpha_V(\text{id}) : U \to V
\beta_U(\text{id}) : V \to U
f = \beta_U(\text{id})
\phi = \text{id}
\alpha_U(\beta_U(\text{id})) = \beta_U(\text{id}) \circ \alpha_V(\text{id})
\alpha_U
\beta_U
 \text{id} = \beta_U(\text{id}) \circ \alpha_V(\text{id})
\beta_Y
f \circ \phi = \beta_Y(f \circ \alpha_X(\phi))
\beta_X
\psi
\phi = \beta_X(\psi)
f \circ \beta_X(\psi) = \beta_Y(f \circ \psi)
f = \alpha_V(\text{id})
\psi = \text{id}
\alpha_V(\text{id}) \circ \beta_U(\text{id}) = \text{id}
\alpha_V(\text{id})
\beta_U(\text{id})
