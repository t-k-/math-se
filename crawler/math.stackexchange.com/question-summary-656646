\Gamma\models A\Rightarrow   \Gamma\cup\{\neg A\}
\Gamma\cup\{\neg A\}
\Rightarrow\Gamma\models A
\Gamma\models A\text{ and }\Gamma\cup\{\neg A\}\text{ is satisfiable}
v^{*}(A)=\text{T}
v^{*}(\neg A)=\text{T}
v^{*}(A)=\text{F}
\mathcal{L}
\Gamma\cup\{\neg A\}\text{ is not satisfiable}\text{ and }\Gamma\nvDash A
\Gamma\cup\{\neg A\}
S\in\Gamma
\{S\}\cup\{\neg A\}
\{S,\neg A\}
A\in\Gamma
\Gamma\models A
\Diamond
\Gamma
A
\Gamma
A
\Gamma\cup\left\{¬A\right\}
\Gamma\cup\left\{¬A\right\}
\Gamma\cup\{\neg A\}
u
u^*(\neg A)=\text{T}\Rightarrow u^*(A)=\text{F}
u
\forall B\in\Gamma, u^*(B)=\text{T}
\Gamma \models A
\Gamma
A
\Gamma \cup \{\neg A\}
\Gamma \cup \{\neg A\}
