\pi = R^n
n
n
tf ^{(new)}
\theta
m
|S|
\theta_j^{(i)}
j
i
\max_{\pi \in R^{|S|}}  \sum_{j = 1}^{m} tf^{(new)}_j log(\sum_{i \in S} \pi_i\theta_j^{(i)})
\sum_{i \in S}\pi_i = 1 \\
\forall i \in S : \pi_1 \geq 0
 tf^{(new)}_1 log(\pi \cdot\theta_1 ) + ... + tf^{(new)}_{m} log(\pi \cdot\theta_{m} )
\theta_j
{\mathbb R}^n
\pi \cdot \theta_1, \ldots, \pi \cdot \theta_m
m
