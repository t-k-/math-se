((\lnot p \wedge q)\wedge (q \vee r))\wedge (\lnot q \wedge r)
\neg q
((\neg p \wedge q)\wedge(q \vee r))\wedge(\neg q \wedge r)= (\neg p \wedge q)\wedge(\neg q \wedge r)= (q\wedge\neg q)\wedge(~p\wedge r)=
((\neg p \wedge q)\wedge(q \vee r))\wedge(\neg q \wedge r)=^{1} (\neg p \wedge q)\wedge ((\neg q\wedge r \wedge q)\vee (r\wedge \neg q\wedge r))=^{2}
(\neg p \wedge q)\wedge (r\wedge \neg q)=\neg p \wedge q\wedge r\wedge \neg q= (\neg q \wedge q)\wedge (r\wedge \neg p )=^{3}CONTRADICTION 
(\neg q\wedge r \wedge q)=(\neg q \wedge q)
((\neg q\wedge r \wedge q)\vee (r\wedge \neg q\wedge r))=(false)\vee (r\wedge \neg q\wedge r)=(r\wedge \neg q\wedge r)
(r\wedge \neg q\wedge r)=(r\wedge \neg q)
\;\land\;
\;\land\;
\;\phi \land \lnot \phi \;\equiv\; \text{false}\;
\;\phi \land \text{false} \;\equiv\; \text{false}\;
