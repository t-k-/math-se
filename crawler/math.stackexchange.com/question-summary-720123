(0-360)
\ x-y'-x''
\ \psi, \theta, \phi
\cos(\theta)\cos(\phi) + \cos(\theta)\sin(\phi) - \sin(\theta)
 \left[ \begin{matrix}    \cos(\theta)\cos(\phi) & \cos(\theta)\sin(\phi) & -\sin(\theta) \\    \sin(\psi)\sin(\theta)\cos(\phi) - \cos(\psi)\sin(\phi) &        \sin(\phi)\sin(\theta)\sin(\psi) + \cos(\phi)\cos(\psi) &        \sin(\psi)\cos(\theta) \\     \cos(\phi)\sin(\theta)\cos(\psi) + \sin(\phi)\sin(\psi) &        \cos(\psi)\sin(\theta)\sin(\phi) - sin(\psi)\cos(\phi) &        \cos(\psi)\cos(\theta) \end{matrix} \right] 
\cos(\psi)\cos(\theta) + (\cos(\psi)\sin(\theta)\sin(\phi) - sin(\psi)\cos(\phi)) + (\cos(\psi)\sin(\theta)\cos(\phi) + \sin(\psi)\sin(\phi))
 \left[ \begin{matrix}    \cos(\psi)\cos(\theta) & \cos(\psi)\sin(\theta)\sin(\phi) - sin(\psi)\cos(\phi) &        \cos(\psi)\sin(\theta)\cos(\phi) + \sin(\psi)\sin(\phi) \\    \sin(\psi)\cos(\theta) & \sin(\psi)\sin(\theta)\sin(\phi) + \cos(\psi)\cos(\phi) &        \sin(\psi)\sin(\theta)\cos(\phi) - \cos(\psi)\sin(\phi) \\    -\sin(\theta) & \cos(\theta)\sin(\phi) & \cos(\theta)\cos(\phi) \\ \end{matrix} \right] 
\cos(\theta)\cos(\phi) + \cos(\theta)\cos(\phi) - \sin(\theta)
\theta
\phi
