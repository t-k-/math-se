\text{adj}(AB) = \text{adj}(B)\text{adj}(A)\:\ (1)
\text{adj}(I) = I\:\ (2)
X \implies Y
A
AX = XA = I
X
\text{adj}(AX) = \text{adj}(XA) = \text{adj}(I)
\text{adj}(X)\text{adj}(A) = \text{adj}(A)\text{adj}(X) = I
\text{adj}(A)
\text{adj}(X)
Y \implies X
\text{adj}(A)
\text{adj}(A)X = X\text{adj}(A) = I
X
X = \text{adj}(B)
(1)
(2)
\text{adj}(BA) = \text{adj}(AB) = \text{adj}(I)
BA = AB = I
A
\text{adj}(A)A=A\text{adj}(A)=\det(A)I.
\text{Adj}(A)
r\leq n-2
\text{Adj}(A)
0
n-1
A
=0
\text{Adj}(A)=0
r=n-1
n-1
n-1
n-1
n-1
\neq 0
\text{Adj}(A)\neq 0
0=\text{det}(A)I=A\times \text{Adj}(A)
\text{Adj}(A)
A
0<\text{rank}(\text{Adj}(A))\leq 1
r=n
\text{det}(A)\neq 0
\text{Adj}(A)
\frac{A}{\text{det(A)}}
Adj(A)
A
