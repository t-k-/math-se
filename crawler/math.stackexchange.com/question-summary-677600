df
M
N
df
TM
\rightarrow
TN
df
(e_1,...,e_n)
U
\subset
M
e(f)
\frac{1}{2}$$\langle(f_{*})e_i,(f_{*})e_i\rangle
df
TM
df$$(p,v)
\langle(f_{*})e_i,(f_{*})e_i\rangle$. I didn't understand how the global differential acts on frames, since the domain of $df$ is $TM$ (so it would have to be something like $df
T:V\to W
V,W
\text{tr}(T^*T)
T^*:W\to V
\langle w,Tv\rangle= \langle T^*w,v\rangle
df:T_xM\to T_{f(x)}N
