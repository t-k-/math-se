f,g:\mathbb{R}^n\rightarrow\mathbb{R}
C^r
h(\textbf{x})=f(\textbf{x})g(\textbf{x})
C^r
r=1
f
C^1
i=1,2,\ldots,n
g
h=fg
\lim_{t\rightarrow 0}\dfrac{f(\textbf{a}+t\textbf{e}_i)-f(\textbf{a})}{t}
\lim_{t\rightarrow 0}\dfrac{f(\textbf{a}+t\textbf{e}_i)(g(\textbf{a}+t\textbf{e}_i)-g(\textbf{a}))+g(\textbf{a})(f(\textbf{a}+t\textbf{e}_i)-f(\textbf{a}))}{t}
f(\textbf{a})\lim_{t\rightarrow 0}\dfrac{g(\textbf{a}+t\textbf{e}_i)-g(\textbf{a})}{t}+\lim_{t\rightarrow 0}\dfrac{f(\textbf{a}+t\textbf{e}_i)-f(\textbf{a})}{t}g(\textbf{a}) = f(\textbf{a})D_ig(\textbf{a})+D_if(\textbf{a})g(\textbf{a})
r
r
r
D_ih
C^{r-1}
i
D_ih(\textbf{a})=f(\textbf{a})D_ig(\textbf{a})+D_if(\textbf{a})g(\textbf{a})
C^{r-1}
C^{r-1}
C^{r-1}
C^{r-1}
