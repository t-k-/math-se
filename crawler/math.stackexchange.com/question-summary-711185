T = \{\langle M, w, r\rangle \mid M \text{ accepts } w^r \text{ when it accepts } w.\}
T := \{\langle M, w, r\rangle \mid M \text{ accepts } w^r \Leftarrow M \text{ accepts } w\}
M
M'
0
M'
00
M'
M
M'
M'
M
M \in H \Leftrightarrow \langle M',0,2\rangle \in T
T
