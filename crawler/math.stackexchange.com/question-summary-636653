P124:
\mathbf{Ax}
C(A)
A
B
M
\mathbb{R^m}.
colspace(A) + colspace(B)
M
\color{green}{A + B}
M
colspace(A) + colspace(B)
M = \begin{bmatrix}     A & B      \end{bmatrix}
A
\alpha
B
\beta
colspace(M) = \sum_{1 \le i \le \alpha + \beta}k_i\cdot\text{column 
 of M} 
= \sum_{1 \; \le \; i  \; \le \; \alpha}k_i\cdot\text{column 
 of A} + \sum_{\alpha + 1 \; \le \; i \; \le \; \alpha + \beta}k_i\cdot\text{column 
 of B} \quad 
k_i \in \mathbb{C}
= colspace(A) + colspace(B) 
\Large{{1.}}
M = \begin{bmatrix}     A & B      \end{bmatrix}
A
B = -A
\mathrm{colspace}(A) = \mathrm{colspace}(B),
\mathrm{colspace}(A) + \mathrm{colspace}(B) = \mathrm{colspace}(A) \not= \{0\},
\mathrm{colspace}(A+B) = \mathrm{colspace}(\mathrm{zero matrix}) = \{0\}.
