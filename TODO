# 1) document formula number as 2nd score dimension

# 2) character difference matters

# 3) ungroup

ignore all len=1 branchwords

return #errors

\def\specialFrac#1#2{\frac{x + #1}{y + #2}}

\rm and \bf using pre-parser

\buildrel ... \over

remove [NULL] 

skip node in branchword: [
