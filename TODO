score function :
	score = (min(d_n, q_n)/q_n * (m+b+1)/(max(d_l, q_l) + 2), num_of_doc_brw)

score_final_sort
 
test 'break;'

ignore all len=1 branchwords

parser return #errors

\def\specialFrac#1#2{\frac{x + #1}{y + #2}}

\rm and \bf using pre-parser

\buildrel ... \over

remove [NULL] 

skip node in branchword: [
